language: elixir

elixir:
  - 1.4.1

services:
  - postgresql
  
before_script:
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.get
  - mix deps.compile
  - mix deps.clean
  - mix ecto.create
  - mix ecto.migrate

script:
  - mix test
