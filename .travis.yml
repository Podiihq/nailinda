language: elixir

elixir:
  - 1.4.1

services:
  - postgresql
  
install:
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.clean ranch
  - mix deps.get

before_script:
  - mix ecto.create
  - mix ecto.migrate

script:
  - mix test
