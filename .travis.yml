language: elixir

elixir:
  - 1.4.1

services:
  - postgresql
  
install:
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.compile
  - mix deps.get

before_script:
  - mix ecto.create, ecto.migrate

script:
  - mix test
